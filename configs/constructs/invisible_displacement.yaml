# Excerpt of configs/taxonomy.yaml. Edit taxonomy first, then re-sync.
invisible_displacement:
  class: 2
  frame_of_reference: egocentric
  definition: >
    Track an object after it is hidden and relocated out of view.
    The question names where it went (landmark or swap partner).
  what_moves: object (while hidden)
  difficulty_axis:
    - delay_steps
    - load_n_objects
    - verbosity
  answer_type:
    - recall_direction: egocentric direction; question names the destination
    - swap: egocentric direction; question names a previously seen partner
  required_temporal_fields:
    - object_state_track
    - displacement_event
    - displacement_candidates
  question_template:
    recall_direction: >
      While out of view, the {object_type} was moved onto {new_location}.
      Where is it relative to you now?
    swap: >
      The {object_type} was moved to where the {other_object_type} used to be.
      Where is the {object_type} relative to you now?
  ground_truth_method: >
    Hidden move from displacement_events. Answer is the object's
    egocentric bearing at the query pose. Floor destinations need a
    nearby named landmark, never a bare Floor. Swap items name a
    previously seen partner instead of a receptacle.
  distractor_pattern:
    - original_location
    - nearby_receptacle
    - salient_decoy_location
  discriminators:
    - queried object was distinguishable, then hidden, then moved
    - the move itself is never visible
    - question names a destination cue (landmark or swap partner)
